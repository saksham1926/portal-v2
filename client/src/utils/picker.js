// Google Drive Picker helper (placeholder).
// Add your OAuth Client ID and API key in .env and expose via VITE_*
export function openDrivePicker({ onPicked }) {
  alert('Google Drive Picker is not configured yet. Add your API keys to enable.')
  // TODO: implement with gapi + google.picker API.
}